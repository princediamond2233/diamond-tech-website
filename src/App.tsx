import React,{useEffect,useMemo,useState} from 'react';
import {motion,AnimatePresence} from 'framer-motion';
import {ArrowRight,BarChart3,BriefcaseBusiness,Check,ChevronRight,CircleUserRound,Code2,Database,FileImage,Globe2,Instagram,LayoutDashboard,Linkedin,Menu,MessageCircle,MonitorSmartphone,Palette,Plus,Search,Send,Settings,ShieldCheck,Smartphone,Sparkles,Star,Trash2,Upload,Users,Video,X,Zap,LogOut,BookOpen,Mail,Eye,EyeOff,RefreshCw,History,Save,GripVertical,MonitorPlay,Copy,Undo2,Clipboard,Paintbrush,Globe,PanelTop,ExternalLink,Command,Keyboard,RotateCcw} from 'lucide-react';
import './styles.css';

type Service={id:string;title:string;description:string;icon:string;imageUrl?:string;image?:string;features:string[];published:boolean;sort_order?:number};
type Project={id:string;title:string;category:string;description:string;content:string;cover_url?:string;gallery:string[];technologies:string[];published:boolean;sort_order?:number};
type Testimonial={id:string;name:string;company:string;text:string;image_url?:string;published:boolean};
type Blog={id:string;title:string;excerpt:string;content:string;cover_url?:string;author:string;tags:string[];seo:any;published:boolean;published_at?:string};
type Media={id:string;name:string;mimeType?:string;url:string;mediaType:'image'|'video'|'file';page:string;section:string;caption:string;altText:string;published?:boolean;createdAt:string};
type PageBlock={id:string;type:'text'|'image'|'video'|'media'|'cta'|'quote'|'stats'|'gallery'|'faq'|'team'|'logos'|'divider'|'spacer'|'services'|'portfolio'|'testimonials'|'founder'|'contact';title:string;text:string;mediaUrl:string;caption:string;page:string;section:string;published:boolean;sortOrder:number;layout?:'wide'|'split'|'card';align?:'left'|'center'|'right';background?:'white'|'soft'|'dark'|'blue';buttonText?:string;buttonUrl?:string;buttonUrl2?:string;statValue?:string;statLabel?:string};
type Design={primary:string;secondary:string;accent:string;success:string;warning:string;danger:string;font:string;borderRadius:string;buttonStyle:string};
type Site={design?:Design;heroTitle:string;heroText:string;heroEyebrow:string;heroImage:string;aboutTitle:string;aboutText:string;pages:{aboutTitle:string;aboutText:string;servicesTitle:string;servicesText:string;portfolioTitle:string;portfolioText:string;solutionsTitle:string;solutionsText:string;processTitle:string;processText:string;blogTitle:string;blogText:string;contactTitle:string;contactText:string};founderName:string;founderTitle:string;founderBio:string;founderImage:string;founderMessage:string;ctaTitle:string;ctaText:string;phone:string;email:string;siteTitle:string;metaDescription:string;services:Service[];projects:Project[];testimonials:Testimonial[];blog:Blog[];media:Media[];pageBlocks:Record<string,PageBlock[]>};
const defaultDesign:Design={primary:'#003366',secondary:'#072A6B',accent:'#CC0000',success:'#28A745',warning:'#FFC107',danger:'#DC3545',font:'Inter',borderRadius:'14px',buttonStyle:'rounded'};
const seed:Site={design:defaultDesign,heroTitle:'Transforming Ideas Into Digital Excellence',heroText:'Diamond Tech builds modern websites, custom software, mobile applications and digital experiences designed to help businesses grow.',heroEyebrow:'SOFTWARE • WEB • MOBILE • DESIGN',heroImage:'/media/diamond-tech-logo.png',pages:{aboutTitle:'Building Technology That Moves Businesses Forward',aboutText:'Diamond Tech is a technology and digital solutions company focused on building practical, modern and professional digital products for businesses, organizations and institutions.',servicesTitle:'Technology and design capabilities for ambitious ideas.',servicesText:'Choose a service or combine several into one complete digital solution.',portfolioTitle:'Digital products built to solve real problems.',portfolioText:'Projects, screenshots and case studies published directly from the Diamond Tech Control Station.',solutionsTitle:'Solutions built for your business.',solutionsText:'Purpose-built systems that connect people, processes and information.',processTitle:'From first idea to launch.',processText:'A clear, collaborative process designed to keep projects moving.',blogTitle:'Ideas, technology and digital growth.',blogText:'Insights and announcements managed from the Diamond Tech Control Station.',contactTitle:"Let's build something great.",contactText:'Tell us what you want to build and we will turn the idea into a clear project conversation.'},aboutTitle:'Building Technology That Moves Businesses Forward',aboutText:'Diamond Tech is a technology and digital solutions company focused on building practical, modern and professional digital products for businesses, organizations and institutions.',founderName:'Prince Diamond',founderTitle:'CEO & Founder, Diamond Tech',founderBio:'Prince Diamond is the founder of Diamond Tech, a technology company focused on turning ideas into practical digital products. His work brings together software engineering, design and business-focused technology.',founderImage:'/media/prince-diamond-founder.jpg',founderMessage:'We believe technology should make work simpler, businesses stronger and ideas easier to bring to life.',ctaTitle:"Have An Idea? Let's Build It.",ctaText:'From websites and software to mobile applications and professional digital design, Diamond Tech can turn your idea into a practical digital solution.',phone:'0756761076',email:'Diamondtech2233@gmail.com',siteTitle:'Diamond Tech | Software, Websites, Mobile Apps & Digital Solutions',metaDescription:'Diamond Tech builds professional websites, custom software, mobile applications, graphic designs, branding and digital solutions for businesses and organizations.',services:[{id:'s1',title:'Website Development',description:'Modern, responsive and high-performance websites for businesses, schools, organizations and brands.',icon:'globe',imageUrl:'/media/diamond-tech-logo.png',features:['Responsive UI/UX','SEO-ready structure','Performance optimization'],published:true},{id:'s2',title:'Custom Software',description:'Business management systems, school systems, desktop applications and custom software solutions.',icon:'code',imageUrl:'/media/diamond-tech-logo.png',features:['Custom workflows','Secure data','Scalable architecture'],published:true},{id:'s3',title:'Mobile Applications',description:'Modern mobile applications with excellent user experiences and practical business functionality.',icon:'mobile',imageUrl:'/media/diamond-tech-logo.png',features:['Cross-platform apps','Modern interfaces','API integration'],published:true},{id:'s4',title:'Graphic Design',description:'Professional posters, flyers, social graphics, advertisements and digital designs.',icon:'palette',imageUrl:'/media/diamond-tech-logo.png',features:['Campaign graphics','Digital artwork','Marketing materials'],published:true},{id:'s5',title:'Branding',description:'Logo design, brand identity, visual systems and professional brand materials.',icon:'sparkles',imageUrl:'/media/diamond-tech-logo.png',features:['Logo systems','Brand guidelines','Visual identity'],published:true},{id:'s6',title:'Digital Solutions',description:'Technology solutions tailored to specific business and organizational requirements.',icon:'zap',imageUrl:'/media/diamond-tech-logo.png',features:['Digital transformation','Automation','Custom platforms'],published:true}],projects:[],testimonials:[],blog:[],media:[],pageBlocks:{home:[],about:[],services:[],portfolio:[],solutions:[],process:[],blog:[],contact:[]}};
const icons:any={globe:Globe2,code:Code2,mobile:Smartphone,palette:Palette,sparkles:Sparkles,zap:Zap};
const builderComponents:any[]=[['text','Text',BookOpen,'Heading and rich copy'],['image','Image',FileImage,'Single image with caption'],['video','Video',MonitorPlay,'Video/media showcase'],['gallery','Gallery',FileImage,'Multi-image visual gallery'],['cta','Call to Action',Send,'Conversion section'],['quote','Quote',MessageCircle,'Client quote or statement'],['stats','Statistics',BarChart3,'Animated business metrics'],['faq','FAQ',BookOpen,'Frequently asked questions'],['team','Team',Users,'Team member cards'],['logos','Logo Strip',ShieldCheck,'Trusted brands / clients'],['services','Services Grid',BriefcaseBusiness,'Live services showcase'],['portfolio','Portfolio Grid',BarChart3,'Live portfolio showcase'],['testimonials','Testimonials',Star,'Live testimonial carousel'],['founder','Founder',CircleUserRound,'CEO / Founder spotlight'],['contact','Contact Form',Mail,'Project enquiry section'],['divider','Divider',ChevronRight,'Visual separator'],['spacer','Spacer',Plus,'Controlled empty space']];
const API=import.meta.env.VITE_API_URL||'http://127.0.0.1:5080';
function wa(phone:string){return `https://wa.me/256${phone.replace(/\D/g,'').replace(/^0/,'')}?text=${encodeURIComponent('Hello Diamond Tech, I would like to discuss a project.')}`}
function normalize(raw:any):Site{const s=raw.settings||{};return {...seed,...s,design:{...defaultDesign,...(s.design||{})},services:(raw.services||[]).map((x:any)=>({...x,imageUrl:x.image_url||x.imageUrl,features:Array.isArray(x.features)?x.features:[]})),projects:raw.projects||[],testimonials:raw.testimonials||[],blog:raw.blog||[],media:raw.media||[],pageBlocks:(s.pageBlocks||{})}}
function App(){const[data,setData]=useState<Site>(seed);const[page,setPage]=useState('home');const[mobile,setMobile]=useState(false);const[cms,setCms]=useState(false);const[token,setToken]=useState(localStorage.getItem('dt-admin-token')||'');const[toast,setToast]=useState('');
 useEffect(()=>{fetch(`${API}/api/site`).then(r=>r.ok?r.json():Promise.reject()).then(x=>setData(normalize(x))).catch(()=>{const l=localStorage.getItem('dt-site');if(l)try{setData(JSON.parse(l))}catch{}})},[]);
 useEffect(()=>{localStorage.setItem('dt-site',JSON.stringify(data));document.title=data.siteTitle},[data]);
 useEffect(()=>{if(toast){const t=setTimeout(()=>setToast(''),2600);return()=>clearTimeout(t)}},[toast]);
 const nav=(p:string)=>{setPage(p);setMobile(false);window.scrollTo({top:0,behavior:'smooth'})};
 const openAdmin=()=>setCms(true); const logout=()=>{localStorage.removeItem('dt-admin-token');setToken('');setCms(false);setToast('Signed out')};
 if(cms&&!token)return <AdminLogin onSuccess={t=>{localStorage.setItem('dt-admin-token',t);setToken(t)}} close={()=>setCms(false)}/>;
 if(cms&&token)return <CMS token={token} data={data} setData={setData} close={()=>setCms(false)} logout={logout} toast={toast} setToast={setToast}/>;
 return <><header className="header"><div className="container nav"><button className="brand" onClick={()=>nav('home')}><img src="/media/diamond-tech-logo.png" alt="Diamond Tech"/><span>DIAMOND<span>TECH</span></span></button><nav className={mobile?'nav-links open':'nav-links'}>{['home','about','services','portfolio','solutions','process','blog','contact'].map(x=><button className={page===x?'active':''} key={x} onClick={()=>nav(x)}>{x[0].toUpperCase()+x.slice(1)}</button>)}</nav><div className="nav-actions"><a className="btn btn-primary small" href={wa(data.phone)} target="_blank" rel="noreferrer">Let's Talk <MessageCircle size={16}/></a><button className="icon-btn mobile-menu" onClick={()=>setMobile(!mobile)} aria-label="Menu">{mobile?<X/>:<Menu/>}</button></div></div></header>
 <main>{page==='home'&&<Home data={data} nav={nav}/>} {page==='about'&&<About data={data} nav={nav}/>} {page==='services'&&<Services data={data} nav={nav}/>} {page==='portfolio'&&<Portfolio data={data} nav={nav}/>} {page==='solutions'&&<Solutions nav={nav}/>} {page==='process'&&<Process/>} {page==='blog'&&<Blog data={data}/>} {page==='contact'&&<Contact data={data} setToast={setToast}/>}<PageBlocks page={page} blocks={data.pageBlocks?.[page]||[]}/></main>
 <footer className="footer"><div className="container footer-grid"><div><img className="footer-logo" src="/media/diamond-tech-logo.png" alt="Diamond Tech logo"/><p>BUILDING DIGITAL EXCELLENCE</p></div><div><h4>Explore</h4>{['about','services','portfolio','solutions','process','blog','contact'].map(x=><button key={x} onClick={()=>nav(x)}>{x}</button>)}</div><div><h4>Services</h4>{data.services.filter(x=>x.published).slice(0,6).map(s=><button key={s.id} onClick={()=>nav('services')}>{s.title}</button>)}</div><div><h4>Contact</h4><a href={`tel:${data.phone}`}>{data.phone}</a><a href={`mailto:${data.email}`}>{data.email}</a><a href={wa(data.phone)} target="_blank" rel="noreferrer">WhatsApp</a></div></div><div className="container copyright"><span>© 2026 Diamond Tech. All Rights Reserved.</span><button onClick={openAdmin}>Admin Control Station</button></div></footer><a className="whatsapp" href={wa(data.phone)} target="_blank" rel="noreferrer" aria-label="WhatsApp"><MessageCircle/></a>{toast&&<div className="toast"><Check size={17}/>{toast}</div>}</>}
function AssignedMediaPage({data,page}:{data:Site;page:string}){const sections=[...new Set(data.media.filter(m=>m.page===page&&(m.published!==false)).map(m=>m.section))];return <>{sections.map(section=><AssignedMedia data={data} page={page} section={section} key={section}/>)}</>}
function AssignedMedia({data,page,section}:{data:Site;page:string;section:string}){const items=data.media.filter(m=>m.page===page&&m.section===section&&(m.published!==false));if(!items.length)return null;return <section className="assigned-media section"><div className="container"><div className="section-kicker">{section.toUpperCase()}</div><div className="assigned-media-grid">{items.map(m=>m.mediaType==='video'?<video key={m.id} controls playsInline src={`${API}${m.url}`} aria-label={m.caption||m.altText}/>:<img key={m.id} src={`${API}${m.url}`} alt={m.altText||m.caption||m.name}/>)}</div>{items.map(m=>m.caption&&<p className="media-caption" key={m.id+'c'}>{m.caption}</p>)}</div></section>}
function Home({data,nav}:{data:Site;nav:(x:string)=>void}){return <><section className="hero"><div className="hero-grid container"><div className="hero-copy"><motion.div initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} className="eyebrow"><Sparkles size={15}/>{data.heroEyebrow}</motion.div><motion.h1 initial={{opacity:0,y:35}} animate={{opacity:1,y:0}} transition={{delay:.08}}>{data.heroTitle}</motion.h1><motion.p initial={{opacity:0,y:25}} animate={{opacity:1,y:0}} transition={{delay:.16}}>{data.heroText}</motion.p><div className="hero-buttons"><button className="btn btn-primary" onClick={()=>nav('contact')}>Start Your Project <ArrowRight/></button><button className="btn btn-outline" onClick={()=>nav('portfolio')}>View Our Work <ChevronRight/></button></div><div className="hero-proof"><span><Check/> Professional Delivery</span><span><ShieldCheck/> Secure & Maintainable</span><span><Zap/> Modern Technology</span></div></div><div className="hero-visual"><div className="orb orb-a"/><div className="orb orb-b"/><motion.div className="device-card" animate={{y:[0,-10,0]}} transition={{duration:5,repeat:Infinity,ease:'easeInOut'}}><div className="device-top"><span/><span/><span/></div><div className="device-screen"><img src={data.heroImage||'/media/diamond-tech-logo.png'} alt="Diamond Tech digital solutions"/><div className="screen-lines"><i/><i/><i/><i/></div></div></motion.div><motion.div className="float-card fc-one" animate={{y:[0,-7,0]}} transition={{duration:4,repeat:Infinity}}><Code2/><div><b>Custom Software</b><small>Built around your workflow</small></div></motion.div><motion.div className="float-card fc-two" animate={{y:[0,7,0]}} transition={{duration:4.6,repeat:Infinity}}><Smartphone/><div><b>Web + Mobile</b><small>Responsive digital experiences</small></div></motion.div></div></div></section><Stats/><section className="section"><div className="container section-heading"><div><div className="section-kicker">OUR SERVICES</div><h2>Digital capabilities, built around real needs.</h2></div><p>From a polished website to a complete business system, Diamond Tech combines engineering, design and practical delivery.</p></div><Services data={data} nav={nav} compact/></section><section className="founder"><div className="container founder-grid"><div className="founder-image"><img src={data.founderImage} alt="Prince Diamond, CEO and Founder of Diamond Tech"/><div className="founder-badge"><CircleUserRound/> CEO & Founder</div></div><div><div className="section-kicker">LEADERSHIP</div><h2>{data.founderName}</h2><h3>{data.founderTitle}</h3><p className="lead">{data.founderBio}</p><blockquote>{data.founderMessage}</blockquote><button className="text-link" onClick={()=>nav('about')}>Meet Diamond Tech <ArrowRight/></button></div></div></section><section className="dark-cta section"><div className="container cta"><div><div className="section-kicker light">LET'S BUILD</div><h2>{data.ctaTitle}</h2><p>{data.ctaText}</p></div><div className="cta-buttons"><button className="btn btn-white" onClick={()=>nav('contact')}>Start Your Project <ArrowRight/></button><a className="btn btn-glass" href={wa(data.phone)} target="_blank" rel="noreferrer">WhatsApp Us <MessageCircle/></a></div></div></section><AssignedMediaPage data={data} page="Home"/></>}
function Stats(){return <section className="stats"><div className="container stats-grid">{[['100+','Projects'],['50+','Clients'],['5+','Years Experience'],['24/7','Digital Support']].map(([a,b])=><div className="stat" key={b}><strong>{a}</strong><span>{b}</span></div>)}</div></section>}
function About({data,nav}:{data:Site;nav:(x:string)=>void}){return <><PageHero kicker="ABOUT DIAMOND TECH" title={data.pages.aboutTitle} text={data.pages.aboutText}/><section className="section"><div className="container split"><div><div className="section-kicker">WHY DIAMOND TECH?</div><h2>More than technology. We build solutions.</h2></div><div className="feature-grid">{[['Innovation','Modern technologies and creative solutions.',Sparkles],['Quality','Professional development and attention to detail.',Check],['Reliability','Solutions designed for real business needs.',ShieldCheck],['Partnership','We work closely with clients from idea to launch.',Users]].map(([a,b,I]:any)=><div className="feature-card" key={a}><I/><h3>{a}</h3><p>{b}</p></div>)}</div></div></section><section className="founder"><div className="container founder-grid"><div className="founder-image"><img src={data.founderImage} alt="Prince Diamond"/></div><div><div className="section-kicker">FOUNDER'S NOTE</div><h2>{data.founderName}</h2><h3>{data.founderTitle}</h3><p className="lead">{data.founderBio}</p><blockquote>{data.founderMessage}</blockquote><button className="btn btn-primary" onClick={()=>nav('contact')}>Work With Diamond Tech <ArrowRight/></button></div></div></section><AssignedMediaPage data={data} page="About"/></>}
function Services({data,nav,compact=false}:{data:Site;nav:(x:string)=>void;compact?:boolean}){const list=data.services.filter(s=>s.published);return <div>{!compact&&<PageHero kicker="OUR DIGITAL SERVICES" title={data.pages.servicesTitle} text={data.pages.servicesText}/>}<div className={compact?'service-grid container':'section'}><div className={compact?'':'container service-grid'}>{list.map((s,i)=>{const I=icons[s.icon]||Code2;return <motion.article className="service-card" key={s.id} whileHover={{y:-7}}><div className="service-media"><img src={s.imageUrl||s.image||'/media/diamond-tech-logo.png'} alt={s.title}/><span>0{i+1}</span></div><div className="service-body"><I/><h3>{s.title}</h3><p>{s.description}</p><div className="feature-list">{s.features.map(f=><span key={f}><Check size={14}/>{f}</span>)}</div><button className="text-link" onClick={()=>nav('contact')}>Learn More <ArrowRight/></button></div></motion.article>})}</div></div>{!compact&&<AssignedMediaPage data={data} page="Services"/>}</div>}
function Portfolio({data,nav}:{data:Site;nav:(x:string)=>void}){return <><PageHero kicker="SELECTED WORK" title={data.pages.portfolioTitle} text={data.pages.portfolioText}/><section className="section"><div className="container portfolio-grid">{data.projects.length?data.projects.filter(x=>x.published).map((p,i)=><article className="project" key={p.id}><div className="project-visual">{p.cover_url?<img src={p.cover_url} alt={p.title}/>:<img src="/media/diamond-tech-logo.png" alt="Diamond Tech"/>}<span>0{i+1}</span></div><div><small>{p.category}</small><h3>{p.title}</h3><p>{p.description}</p><button className="text-link" onClick={()=>nav('contact')}>Discuss a similar project <ArrowRight/></button></div></article>):<div className="empty-blog" style={{gridColumn:'1/-1'}}><BriefcaseBusiness/><h3>Portfolio is ready for your projects.</h3><p>Publish project case studies, images, galleries and technologies from the CMS.</p></div>}</div></section></>}
function Solutions({nav}:{nav:(x:string)=>void}){const x=['School Management Systems','Business Management Systems','Inventory Systems','Finance Systems','Customer Management Systems','Desktop Applications','Web Applications','Mobile Applications','Custom Digital Platforms'];return <><PageHero kicker="SOLUTIONS" title={data.pages.solutionsTitle} text={data.pages.solutionsText}/><section className="section"><div className="container solution-grid">{x.map((s,i)=><div className="solution" key={s}><div className="num">0{i+1}</div><MonitorSmartphone/><h3>{s}</h3><p>Designed for your workflows, users and growth.</p><button onClick={()=>nav('contact')}>Learn More <ArrowRight size={16}/></button></div>)}</div></section></>}
function Process(){const x=[['DISCOVER','Understand your idea, business and requirements.'],['PLAN','Create the project structure, technology plan and user experience.'],['DESIGN','Create modern UI/UX designs and visual systems.'],['BUILD','Develop and integrate the complete solution.'],['TEST','Test performance, responsiveness, security and functionality.'],['LAUNCH','Deploy the final solution and provide support.']];return <><PageHero kicker="OUR PROCESS" title={data.pages.processTitle} text={data.pages.processText}/><section className="section"><div className="container timeline">{x.map((a,i)=><div className="step" key={a[0]}><div className="step-num">0{i+1}</div><div><small>{a[0]}</small><h3>{a[1]}</h3></div></div>)}</div></section></>}
function Blog({data}:{data:Site}){return <><PageHero kicker="BLOG / NEWS" title={data.pages.blogTitle} text={data.pages.blogText}/><section className="section"><div className="container portfolio-grid">{data.blog.filter(x=>x.published).map(p=><article className="project" key={p.id}><div className="project-visual">{p.cover_url?<img src={p.cover_url} alt={p.title}/>:<img src="/media/diamond-tech-logo.png" alt="Diamond Tech"/>}</div><div><small>{p.author}</small><h3>{p.title}</h3><p>{p.excerpt}</p><span className="text-link">Read article <ArrowRight/></span></div></article>)}{!data.blog.filter(x=>x.published).length&&<div className="empty-blog" style={{gridColumn:'1/-1'}}><BookOpen/><h3>Your editorial space is ready.</h3><p>Create and publish articles, cover images, tags and SEO metadata in the CMS.</p></div>}</div></section><AssignedMediaPage data={data} page="Blog"/></>}
function Contact({data,setToast}:{data:Site;setToast:(x:string)=>void}){const[sent,setSent]=useState(false);const[submitting,setSubmitting]=useState(false);const submit=async(e:any)=>{e.preventDefault();setSubmitting(true);const fd=new FormData(e.currentTarget);const body=Object.fromEntries(fd.entries());try{const r=await fetch(`${API}/api/messages`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({fullName:body.fullName,email:body.email,phone:body.phone||'',service:body.service||'',budget:body.budget||'',message:body.message})});if(!r.ok)throw new Error();setSent(true);setToast('Project request received.')}catch{setToast('Could not send right now. Please use WhatsApp.')}finally{setSubmitting(false)}};return <><PageHero kicker="CONTACT DIAMOND TECH" title={data.pages.contactTitle} text={data.pages.contactText}/><section className="section"><div className="container contact-grid"><div className="contact-info"><h2>Start a project</h2><p className="lead">Choose a service, describe the goal and send your request.</p><a href={wa(data.phone)} target="_blank" rel="noreferrer"><MessageCircle/> WhatsApp {data.phone}</a><a href={`mailto:${data.email}`}><Mail/> {data.email}</a></div><form className="form-card" onSubmit={submit}>{sent?<div className="success"><Check/><h3>Thank you.</h3><p>Your project request has been received.</p><button type="button" className="btn btn-primary" onClick={()=>setSent(false)}>Send Another</button></div>:<><label>Full Name<input name="fullName" required placeholder="Your name"/></label><div className="form-row"><label>Email<input name="email" required type="email" placeholder="you@example.com"/></label><label>Phone<input name="phone" placeholder="Phone number"/></label></div><label>Service Required<select name="service" required><option value="">Select a service</option>{data.services.map(s=><option key={s.id}>{s.title}</option>)}</select></label><label>Project Budget<select name="budget"><option>Not sure yet</option><option>UGX 500,000 – 2,000,000</option><option>UGX 2,000,000 – 5,000,000</option><option>UGX 5,000,000+</option></select></label><label>Message<textarea name="message" required rows={6} placeholder="Tell us about your project..."/></label><button className="btn btn-primary" disabled={submitting}>{submitting?<RefreshCw className="spin"/>:<Send/>}{submitting?'Sending…':'Send Project Request'}</button></>}</form></div></section></>}
function PageHero({kicker,title,text}:{kicker:string;title:string;text:string}){return <section className="page-hero"><div className="container"><div className="section-kicker">{kicker}</div><h1>{title}</h1><p>{text}</p></div></section>}
function AdminLogin({onSuccess,close}:{onSuccess:(t:string)=>void;close:()=>void}){const[u,setU]=useState('admin');const[p,setP]=useState('');const[show,setShow]=useState(false);const[err,setErr]=useState('');const[busy,setBusy]=useState(false);const go=async(e:any)=>{e.preventDefault();setBusy(true);setErr('');try{const r=await fetch(`${API}/api/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:u,password:p})});const x=await r.json();if(!r.ok)throw new Error(x.error||'Login failed');onSuccess(x.token)}catch(e:any){setErr(e.message)}finally{setBusy(false)}};return <div className="admin-gate"><div className="login-card"><button className="login-close" onClick={close}><X/></button><img src="/media/diamond-tech-logo.png" alt="Diamond Tech"/><div className="section-kicker">CONTROL STATION</div><h1>Admin Sign In</h1><p>Secure access to your Diamond Tech website.</p><form onSubmit={go}><label>Username<input value={u} onChange={e=>setU(e.target.value)} autoComplete="username"/></label><label>Password<div className="password-wrap"><input value={p} onChange={e=>setP(e.target.value)} type={show?'text':'password'} autoComplete="current-password" required/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label>{err&&<div className="error-box">{err}</div>}<button className="btn btn-primary" disabled={busy}>{busy?<RefreshCw className="spin"/>:<ShieldCheck/>}{busy?'Signing in…':'Sign In'}</button></form><small>Use the administrator credentials configured on the server.</small></div></div>}
function CMS({token,data,setData,close,logout,setToast}:{token:string;data:Site;setData:React.Dispatch<React.SetStateAction<Site>>;close:()=>void;logout:()=>void;toast:string;setToast:(s:string)=>void}){
 const[tab,setTab]=useState('overview');const[busy,setBusy]=useState(false);const[search,setSearch]=useState('');const[health,setHealth]=useState<any>(null);
 const headers={Authorization:`Bearer ${token}`,'Content-Type':'application/json'};const get=async(path:string)=>{const r=await fetch(`${API}${path}`,{headers:{Authorization:`Bearer ${token}`}});if(r.status===401){logout();throw new Error('Session expired')}if(!r.ok)throw new Error('Request failed');return r.json()};
 const saveSettings=async()=>{setBusy(true);try{const body={heroTitle:data.heroTitle,heroText:data.heroText,heroEyebrow:data.heroEyebrow,heroImage:data.heroImage,pages:data.pages,aboutTitle:data.aboutTitle,aboutText:data.aboutText,founderName:data.founderName,founderTitle:data.founderTitle,founderBio:data.founderBio,founderImage:data.founderImage,founderMessage:data.founderMessage,ctaTitle:data.ctaTitle,ctaText:data.ctaText,phone:data.phone,email:data.email,siteTitle:data.siteTitle,metaDescription:data.metaDescription,pageBlocks:data.pageBlocks};await fetch(`${API}/api/admin/settings`,{method:'PUT',headers,body:JSON.stringify(body)});setToast('Website settings saved')}catch{setToast('Save failed')}finally{setBusy(false)}};
 const load=async()=>{try{const x=await get('/api/site');setData(normalize(x));const h=await get('/api/admin/dashboard');setHealth(h)}catch{}};
 useEffect(()=>{load()},[]);
 return <div className="cms"><aside className="cms-side"><div className="cms-brand"><img src="/media/diamond-tech-logo.png" alt="Diamond Tech"/><div><b>DIAMOND TECH</b><small>CONTROL STATION</small></div></div>{[['overview','Overview',LayoutDashboard],['page-studio','Page Studio',MonitorPlay],['website','Website',Globe2],['services','Services',BriefcaseBusiness],['portfolio','Portfolio',BarChart3],['testimonials','Testimonials',Star],['founder','CEO / Founder',CircleUserRound],['media','Media Library',FileImage],['blog','Blog / News',BookOpen],['messages','Messages',MessageCircle],['audit','Audit Trail',History],['seo','SEO',Search],['settings','Settings',Settings]].map(([id,label,I]:any)=><button key={id} className={tab===id?'selected':''} onClick={()=>setTab(id)}><I size={18}/>{label}</button>)}<div className="cms-side-bottom"><button onClick={load}><RefreshCw size={17}/>Refresh</button><button onClick={logout}><LogOut size={17}/>Sign Out</button><button onClick={close}><X size={17}/>Website</button></div></aside><section className="cms-main"><header className="cms-top"><div><div className="section-kicker">WEBSITE CONTROL STATION</div><h1>{tab[0].toUpperCase()+tab.slice(1)}</h1></div><div className="cms-top-actions"><span className="live-dot">LIVE</span>{['website','founder','settings','seo'].includes(tab)&&<button className="btn btn-primary" onClick={saveSettings} disabled={busy}>{busy?<RefreshCw className="spin"/>:<Save/>}Save Changes</button>}</div></header><div className="cms-content">{tab==='overview'&&<Dashboard health={health} setTab={setTab}/>} {tab==='page-studio'&&<PageStudio token={token} data={data} setData={setData} setToast={setToast}/>} {tab==='website'&&<WebsiteControl data={data} setData={setData}/>} {tab==='services'&&<ServicesCMS token={token} data={data} setData={setData} search={search} setSearch={setSearch} setToast={setToast}/>} {tab==='portfolio'&&<ProjectsCMS token={token} search={search} setSearch={setSearch} setToast={setToast}/>} {tab==='testimonials'&&<TestimonialsCMS token={token} setToast={setToast}/>} {tab==='founder'&&<FounderControl data={data} setData={setData}/>} {tab==='media'&&<MediaCMS token={token} setToast={setToast}/>} {tab==='blog'&&<BlogCMS token={token} setToast={setToast}/>} {tab==='messages'&&<MessagesCMS token={token}/>} {tab==='audit'&&<AuditCMS token={token}/>} {tab==='seo'&&<SEOControl data={data} setData={setData}/>} {tab==='settings'&&<SettingsControl data={data} setData={setData}/>}</div></section></div>}
function Dashboard({health,setTab}:{health:any;setTab:(x:string)=>void}){const cards=[['Services',health?.services||0,BriefcaseBusiness,'services'],['Projects',health?.projects||0,BarChart3,'portfolio'],['Testimonials',health?.testimonials||0,Star,'testimonials'],['Media',health?.media||0,FileImage,'media'],['Blog Posts',health?.blog||0,BookOpen,'blog'],['New Messages',health?.new_messages||0,MessageCircle,'messages']];return <><div className="control-grid">{cards.map(([a,b,I,t]:any)=><button className="control-card" key={a} onClick={()=>setTab(t)}><I/><span>{a}</span><strong>{b}</strong><small>Manage <ArrowRight/></small></button>)}</div><div className="panel"><div className="panel-title"><div><h2>Control Station</h2><p>Manage the public website without editing source code.</p></div><ShieldCheck/></div><div className="health"><span><i/> PostgreSQL CMS</span><span><i/> Protected admin API</span><span><i/> Media storage</span><span><i/> Audit logging</span></div></div></>}
function PageBlocks({page,blocks,preview=false,data}:{page:string;blocks:PageBlock[];preview?:boolean;data?:Site}){
 const visible=blocks.filter(b=>b.published).sort((a,b)=>a.sortOrder-b.sortOrder); if(!visible.length)return null;
 const render=(b:PageBlock)=>{
  if(b.type==='services'&&data)return <div className="live-grid">{data.services.filter(x=>x.published).slice(0,6).map(x=><div className="live-card" key={x.id}><img src={x.imageUrl||'/media/diamond-tech-logo.png'} alt={x.title}/><h4>{x.title}</h4><p>{x.description}</p></div>)}</div>;
  if(b.type==='portfolio'&&data)return <div className="live-grid">{data.projects.filter(x=>x.published).slice(0,6).map(x=><div className="live-card" key={x.id}><img src={x.cover_url||'/media/diamond-tech-logo.png'} alt={x.title}/><h4>{x.title}</h4><p>{x.description}</p></div>)}</div>;
  if(b.type==='testimonials'&&data)return <div className="live-quotes">{data.testimonials.filter(x=>x.published).slice(0,3).map(x=><blockquote key={x.id}>“{x.text}”<footer>{x.name} · {x.company}</footer></blockquote>)}</div>;
  if(b.type==='founder'&&data)return <div className="live-founder"><img src={data.founderImage} alt={data.founderName}/><div><h3>{data.founderName}</h3><strong>{data.founderTitle}</strong><p>{data.founderBio}</p></div></div>;
  if(b.type==='gallery'){const urls=(b.mediaUrl||'').split('|').filter(Boolean);return <div className="live-gallery">{urls.map((u,i)=><img key={i} src={u} alt={b.caption||`Gallery image ${i+1}`}/>)}</div>}
  if(b.type==='faq')return <div className="live-faq"><details open><summary>{b.title||'Frequently Asked Question'}</summary><p>{b.text}</p></details></div>;
  if(b.type==='team')return <div className="live-team"><div><div className="avatar-placeholder">DT</div><h4>{b.title}</h4><p>{b.text}</p></div></div>;
  if(b.type==='logos')return <div className="live-logos">{(b.text||'Diamond Tech|Trusted Partner|Digital Excellence').split('|').map(x=><span key={x}>{x}</span>)}</div>;
  if(b.type==='divider')return <hr/>;
  if(b.type==='spacer')return <div className="visual-spacer" style={{height:Math.max(20,Number(b.statValue)||80)}}/>;
  if(b.type==='contact')return <div className="mini-contact"><input placeholder="Full Name"/><input placeholder="Email"/><textarea placeholder="Tell us about your project"/><button className="btn btn-primary">Send Project Request</button></div>;
  return <>
   {b.mediaUrl&&['image','video','media'].includes(b.type)&&(b.type==='video'?<video src={b.mediaUrl} controls/>:<img src={b.mediaUrl} alt={b.caption||b.title}/>)}
   {b.type==='stats'?<div className="stat-display"><strong>{b.statValue||'100+'}</strong><span>{b.statLabel||b.title}</span></div>:b.type==='quote'?<blockquote className="visual-quote">{b.text}</blockquote>:b.type!=='cta'&&b.title&&<h3>{b.title}</h3>}
   {b.type!=='cta'&&b.type!=='quote'&&b.type!=='stats'&&b.text&&<p className="dynamic-copy">{b.text}</p>}
   {b.caption&&<small className="media-caption">{b.caption}</small>}
   {b.type==='cta'&&<div><h3>{b.title}</h3><p>{b.text}</p></div>}
   {b.buttonText&&b.buttonUrl&&<a className="btn btn-primary dynamic-button" href={b.buttonUrl}>{b.buttonText}<ArrowRight size={17}/></a>}
  </>
 };
 return <section className={`dynamic-sections ${preview?'preview-mode':''}`}><div className="container">{visible.map(b=><motion.article key={b.id} className={`dynamic-block block-${b.type} layout-${b.layout||'wide'} bg-${b.background||'white'} align-${b.align||'left'}`} initial={preview?false:{opacity:0,y:28}} whileInView={preview?undefined:{opacity:1,y:0}} viewport={{once:true,amount:.15}}><div className="dynamic-inner">{render(b)}</div></motion.article>)}</div></section>
}
function PageStudio({token,data,setData,setToast}:{token:string;data:Site;setData:React.Dispatch<React.SetStateAction<Site>>;setToast:(s:string)=>void}){
 const pages=['home','about','services','portfolio','solutions','process','blog','contact'];
 const[page,setPage]=useState('home'); const[media,setMedia]=useState<Media[]>(data.media||[]); const[drag,setDrag]=useState<string|null>(null); const[device,setDevice]=useState<'desktop'|'tablet'|'mobile'>('desktop'); const[history,setHistory]=useState<any[]>([]); const[busy,setBusy]=useState(false); const[dirty,setDirty]=useState(false); const[undoStack,setUndoStack]=useState<PageBlock[][]>([]); const[redoStack,setRedoStack]=useState<PageBlock[][]>([]); const[selected,setSelected]=useState<string|null>(null); const[mediaPicker,setMediaPicker]=useState(false); const[mediaFilter,setMediaFilter]=useState(''); const[templateMenu,setTemplateMenu]=useState(false);
 const [studioTab,setStudioTab]=useState<'components'|'design'|'seo'|'reusable'>('components'); const [clipboard,setClipboard]=useState<PageBlock|null>(()=>{try{return JSON.parse(localStorage.getItem('dt-builder-clipboard')||'null')}catch{return null}}); const [showPreview,setShowPreview]=useState(false);
 const design=data.design||defaultDesign;
 const blocks=data.pageBlocks?.[page]||[]; const selectedBlock=blocks.find(x=>x.id===selected)||null; const headers={Authorization:`Bearer ${token}`,'Content-Type':'application/json'};
 useEffect(()=>{fetch(`${API}/api/admin/media`,{headers:{Authorization:`Bearer ${token}`}}).then(r=>r.ok?r.json():[]).then(setMedia).catch(()=>{});fetch(`${API}/api/admin/revisions`,{headers:{Authorization:`Bearer ${token}`}}).then(r=>r.ok?r.json():[]).then(setHistory).catch(()=>{})},[]);
 useEffect(()=>{const fn=(e:KeyboardEvent)=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='s'){e.preventDefault();save(false)} if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='z'){e.preventDefault();undo()} if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='y'){e.preventDefault();redo()} if(e.key==='Escape')setMediaPicker(false)};window.addEventListener('keydown',fn);return()=>window.removeEventListener('keydown',fn)},[blocks,undoStack,redoStack,data,page]);
 const update=(next:PageBlock[],record=true)=>{if(record){setUndoStack(x=>[...x,blocks].slice(-40));setRedoStack([])};setDirty(true);setData(d=>({...d,pageBlocks:{...d.pageBlocks,[page]:next.map((x,i)=>({...x,sortOrder:i}))}}))};
 const patch=(id:string,key:keyof PageBlock,value:any)=>update(blocks.map(x=>x.id===id?{...x,[key]:value}:x));
 const make=(type:PageBlock['type'],over:any={})=>{const m=media.find(x=>type==='video'?x.mediaType==='video':x.mediaType==='image');return {id:`block-${Date.now()}-${Math.random().toString(36).slice(2,7)}`,type,title:type==='text'?'New text section':type==='image'?'New image section':type==='video'?'New video section':type==='gallery'?'Image gallery':type==='cta'?'New call to action':type==='quote'?'Client quote':type==='stats'?'Business statistic':type==='faq'?'Frequently Asked Questions':type==='team'?'Team section':type==='logos'?'Trusted by':type==='founder'?'Meet the Founder':type==='contact'?'Start a conversation':type==='services'?'Our Services':type==='portfolio'?'Selected Work':type==='testimonials'?'Client Testimonials':'',text:type==='text'?'Add your content here.':type==='cta'?'Add a strong invitation for visitors.':type==='quote'?'Add a testimonial or statement.':type==='stats'?'Projects':type==='faq'?'Answer a common client question.':type==='team'?'Add team member details.':type==='logos'?'Diamond Tech|Trusted Partner|Digital Excellence':'',mediaUrl:m?.url||'',caption:'',page,section:'Custom Section',published:false,sortOrder:blocks.length,layout:type==='divider'||type==='spacer'?'wide':'split',align:'left',background:type==='cta'?'blue':'white',buttonText:type==='cta'?'Start Your Project':'',buttonUrl:type==='cta'?'/contact':'',buttonUrl2:'',statValue:type==='stats'?'100+':type==='spacer'?'80':'',statLabel:type==='stats'?'Projects':'',...over} as PageBlock};
 const add=(type:PageBlock['type'])=>{const b=make(type);update([...blocks,b]);setSelected(b.id);setToast(`${type} section added`)};
 const remove=(id:string)=>{update(blocks.filter(x=>x.id!==id));if(selected===id)setSelected(null)};
 const duplicate=(id:string)=>{const b=blocks.find(x=>x.id===id);if(!b)return;const copy={...b,id:`block-${Date.now()}-${Math.random().toString(36).slice(2,7)}`,title:`${b.title} Copy`,published:false};const i=blocks.findIndex(x=>x.id===id);update([...blocks.slice(0,i+1),copy,...blocks.slice(i+1)]);setSelected(copy.id)};
 const move=(id:string,to:number)=>{const arr=[...blocks];const from=arr.findIndex(x=>x.id===id);if(from<0)return;const[item]=arr.splice(from,1);arr.splice(Math.max(0,Math.min(to,arr.length)),0,item);update(arr)};
 const undo=()=>{if(!undoStack.length)return;const previous=undoStack[undoStack.length-1];setRedoStack(x=>[...x,blocks].slice(-40));setUndoStack(x=>x.slice(0,-1));setDirty(true);setData(d=>({...d,pageBlocks:{...d.pageBlocks,[page]:previous}}));};
 const redo=()=>{if(!redoStack.length)return;const next=redoStack[redoStack.length-1];setUndoStack(x=>[...x,blocks].slice(-40));setRedoStack(x=>x.slice(0,-1));setDirty(true);setData(d=>({...d,pageBlocks:{...d.pageBlocks,[page]:next}}));};
 const save=async(publish=true)=>{setBusy(true);try{const current={...data,pageBlocks:{...data.pageBlocks,[page]:blocks}};const rr=await fetch(`${API}/api/admin/revisions`,{method:'POST',headers,body:JSON.stringify({label:`${publish?'Published':'Draft'} — ${page}`,data:current})});if(!rr.ok)throw 0;const r=await fetch(`${API}/api/admin/settings`,{method:'PUT',headers,body:JSON.stringify(current)});if(!r.ok)throw 0;setDirty(false);setToast(publish?'Published successfully':'Draft saved')}catch{setToast('Could not save page')}finally{setBusy(false)}};
 const restore=async(id:string)=>{if(!confirm('Restore this revision?'))return;const r=await fetch(`${API}/api/admin/revisions/${id}/restore`,{method:'POST',headers});if(r.ok){const x=await r.json();setData(normalize(x));setDirty(false);setToast('Revision restored')}};
 const applyTemplate=(kind:string)=>{const t:any={
  landing:[make('text',{title:'Transform Ideas Into Digital Excellence',text:'A premium introduction for your business, product or service.',background:'white',layout:'split',published:true}),make('stats',{title:'Built for growth',statValue:'100+',statLabel:'Projects',published:true}),make('services',{title:'What We Do',text:'Explore our digital capabilities.',layout:'wide',published:true}),make('cta',{title:"Have An Idea? Let's Build It.",text:'Tell us what you want to build.',buttonText:'Let’s Talk',buttonUrl:'/contact',published:true})],
  about:[make('text',{title:'About Diamond Tech',text:data.aboutText,layout:'split',published:true}),make('founder',{title:data.founderName,text:data.founderBio,mediaUrl:data.founderImage,layout:'split',published:true}),make('logos',{title:'Trusted Digital Partner',text:'Diamond Tech|Technology|Design|Digital Excellence',published:true})],
  services:[make('text',{title:data.pages.servicesTitle,text:data.pages.servicesText,layout:'wide',published:true}),make('services',{title:'Our Services',text:'Technology and design capabilities for ambitious ideas.',layout:'wide',published:true}),make('cta',{title:'Need a custom solution?',text:'Let’s discuss your project.',buttonText:'Start Your Project',buttonUrl:'/contact',published:true})],
  portfolio:[make('text',{title:data.pages.portfolioTitle,text:data.pages.portfolioText,layout:'wide',published:true}),make('portfolio',{title:'Selected Work',text:'Projects published from the Control Station.',layout:'wide',published:true}),make('testimonials',{title:'What Clients Say',layout:'wide',published:true})],
  contact:[make('text',{title:data.pages.contactTitle,text:data.pages.contactText,layout:'wide',published:true}),make('contact',{title:'Tell Us About Your Project',text:'Share your goals, timeline and requirements.',layout:'wide',published:true})]
 }[kind]; if(!t)return; setUndoStack(x=>[...x,blocks].slice(-40));setRedoStack([]);setData(d=>({...d,pageBlocks:{...d.pageBlocks,[page]:t.map((x:any,i:number)=>({...x,sortOrder:i}))}}));setSelected(t[0]?.id||null);setDirty(true);setTemplateMenu(false);setToast(`${kind} template applied`)};
 const chooseMedia=(m:Media)=>{if(selectedBlock)patch(selectedBlock.id,'mediaUrl',m.url);setMediaPicker(false);setMediaFilter('')};
 const filteredMedia=media.filter(m=>(!mediaFilter||`${m.name} ${m.caption} ${m.page} ${m.section}`.toLowerCase().includes(mediaFilter.toLowerCase())) && (selectedBlock?.type==='video'?m.mediaType==='video':true));
 const setDesign=(key:keyof Design,value:string)=>{setData(d=>({...d,design:{...defaultDesign,...(d.design||{}),[key]:value}}));setDirty(true)};
 const copyBlock=(id:string)=>{const b=blocks.find(x=>x.id===id);if(!b)return;const c={...b};setClipboard(c);localStorage.setItem('dt-builder-clipboard',JSON.stringify(c));setToast('Section copied')};
 const pasteBlock=()=>{if(!clipboard)return;const b={...clipboard,id:`block-${Date.now()}-${Math.random().toString(36).slice(2,7)}`,page,section:'Pasted Section',published:false};update([...blocks,b]);setSelected(b.id);setToast('Section pasted as draft')};
 const saveReusable=()=>{if(!selectedBlock)return;const key='dt-reusable-blocks';let list:any[]=[];try{list=JSON.parse(localStorage.getItem(key)||'[]')}catch{};list=[...list.filter(x=>x.id!==selectedBlock.id),selectedBlock].slice(-20);localStorage.setItem(key,JSON.stringify(list));setToast('Saved to reusable sections')};
 const reusable=useMemo(()=>{try{return JSON.parse(localStorage.getItem('dt-reusable-blocks')||'[]')}catch{return []}},[selected]);
 const useReusable=(b:any)=>{const n={...b,id:`block-${Date.now()}-${Math.random().toString(36).slice(2,7)}`,page,published:false,section:'Reusable Section'};update([...blocks,n]);setSelected(n.id);setToast('Reusable section added as draft')};
 return <div className="page-studio v7-studio">
  <div className="studio-toolbar"><div><div className="section-kicker">VISUAL WEBSITE BUILDER · V7</div><h2>Diamond Tech Page Studio</h2><p>Build pages visually with templates, reusable sections, media selection, responsive preview and safe publishing.</p></div><div className="studio-actions"><select value={page} onChange={e=>{setPage(e.target.value);setSelected(null);setUndoStack([]);setRedoStack([])}}>{pages.map(p=><option key={p} value={p}>{p[0].toUpperCase()+p.slice(1)}</option>)}</select><button className="btn btn-soft" onClick={()=>setTemplateMenu(v=>!v)}><LayoutDashboard/>Templates</button><button className="btn btn-soft" onClick={undo} disabled={!undoStack.length}><Undo2/>Undo</button><button className="btn btn-soft" onClick={redo} disabled={!redoStack.length}><RefreshCw/>Redo</button><button className="btn btn-soft" onClick={()=>save(false)} disabled={busy}><Save/>Save Draft</button><button className="btn btn-primary" onClick={()=>save(true)} disabled={busy}>{busy?<RefreshCw className="spin"/>:<Zap/>}Publish</button></div></div>
  {templateMenu&&<div className="template-bar panel"><div><b>Start from a section set</b><small>Templates replace the current page canvas. Save a draft first if you need the existing layout.</small></div><div className="template-actions"><button className="btn btn-outline" onClick={()=>applyTemplate('landing')}>Landing Page</button><button className="btn btn-outline" onClick={()=>applyTemplate('about')}>About</button><button className="btn btn-outline" onClick={()=>applyTemplate('services')}>Services</button><button className="btn btn-outline" onClick={()=>applyTemplate('portfolio')}>Portfolio</button><button className="btn btn-outline" onClick={()=>applyTemplate('contact')}>Contact</button></div></div>}
  <div className="v7-builder"><aside className="component-palette panel"><div className="studio-tabs"><button className={studioTab==='components'?'active':''} onClick={()=>setStudioTab('components')}><Plus/>Components</button><button className={studioTab==='design'?'active':''} onClick={()=>setStudioTab('design')}><Paintbrush/>Design</button><button className={studioTab==='seo'?'active':''} onClick={()=>setStudioTab('seo')}><Globe/>SEO</button><button className={studioTab==='reusable'?'active':''} onClick={()=>setStudioTab('reusable')}><Clipboard/>Reusable</button></div>{studioTab==='components'&&<><div className="panel-title"><div><div className="section-kicker">COMPONENTS</div><h3>Build your page</h3></div><Plus/></div><div className="component-list">{builderComponents.map(([type,label,I,desc])=><button key={type} className="component-tile" onClick={()=>add(type)}><I/><span><b>{label}</b><small>{desc}</small></span><Plus/></button>)}</div><div className="palette-note"><ShieldCheck/><span><b>V8 workflow</b><small>Build, style, optimize SEO, reuse sections, preview, then save or publish.</small></span></div></>}{studioTab==='design'&&<div className="studio-panel-content"><h3>Global Design System</h3><p>Changes apply across the website.</p><label>Primary color<input type="color" value={design.primary} onChange={e=>setDesign('primary',e.target.value)}/></label><label>Secondary color<input type="color" value={design.secondary} onChange={e=>setDesign('secondary',e.target.value)}/></label><label>Accent color<input type="color" value={design.accent} onChange={e=>setDesign('accent',e.target.value)}/></label><label>Font<select value={design.font} onChange={e=>setDesign('font',e.target.value)}><option>Inter</option><option>Manrope</option><option>Plus Jakarta Sans</option></select></label><label>Corner radius<select value={design.borderRadius} onChange={e=>setDesign('borderRadius',e.target.value)}><option value="8px">Compact</option><option value="14px">Modern</option><option value="22px">Soft</option></select></label></div>}{studioTab==='seo'&&<div className="studio-panel-content"><h3>Website SEO</h3><label>Site title<input value={data.siteTitle} onChange={e=>{setData(d=>({...d,siteTitle:e.target.value}));setDirty(true)}}/></label><label>Meta description<textarea rows={5} value={data.metaDescription} onChange={e=>{setData(d=>({...d,metaDescription:e.target.value}));setDirty(true)}}/></label><div className="seo-check"><b>Title length</b><span>{data.siteTitle.length}/60</span></div><div className="seo-check"><b>Description length</b><span>{data.metaDescription.length}/160</span></div></div>}{studioTab==='reusable'&&<div className="studio-panel-content"><h3>Reusable Sections</h3><p>Save a selected section and reuse it on another page.</p><button className="btn btn-primary" disabled={!selectedBlock} onClick={saveReusable}><Clipboard/>Save Selected</button><div className="reusable-list">{reusable.length?reusable.map((b:any)=><button key={b.id} className="reusable-item" onClick={()=>useReusable(b)}><b>{b.title||b.type}</b><small>{b.type} · add to {page}</small></button>):<div className="empty-blog"><Clipboard/><p>No reusable sections yet.</p></div>}</div></div>}</aside>
   <section className="canvas-area"><div className="canvas-head"><div><b>{page[0].toUpperCase()+page.slice(1)} Canvas</b><span>{blocks.length} section{blocks.length===1?'':'s'} · {dirty?'Unsaved changes':'All changes saved'}</span></div><div className="preview-switch"><button className={device==='desktop'?'active':''} onClick={()=>setDevice('desktop')}><MonitorPlay/>Desktop</button><button className={device==='tablet'?'active':''} onClick={()=>setDevice('tablet')}><MonitorPlay/>Tablet</button><button className={device==='mobile'?'active':''} onClick={()=>setDevice('mobile')}><Smartphone/>Mobile</button></div></div>
   <div className="canvas-stack">{blocks.map((b,i)=><div key={b.id} className={`canvas-block ${!b.published?'is-draft':''} ${selected===b.id?'is-selected':''}`} draggable onClick={()=>setSelected(b.id)} onDragStart={()=>setDrag(b.id)} onDragOver={e=>e.preventDefault()} onDrop={()=>{if(drag)move(drag,i);setDrag(null)}}><div className="canvas-block-bar"><div className="canvas-label"><GripVertical className="drag-handle"/><span>{String(i+1).padStart(2,'0')}</span><b>{b.type.toUpperCase()}</b>{!b.published&&<em>DRAFT</em>}</div><div className="canvas-tools"><button title="Move up" onClick={e=>{e.stopPropagation();move(b.id,Math.max(0,i-1))}}>↑</button><button title="Move down" onClick={e=>{e.stopPropagation();move(b.id,Math.min(blocks.length-1,i+1))}}>↓</button><button title="Copy" onClick={e=>{e.stopPropagation();copyBlock(b.id)}}><Clipboard size={15}/></button><button title="Duplicate" onClick={e=>{e.stopPropagation();duplicate(b.id)}}><Copy size={15}/></button><button title={b.published?'Hide':'Show'} onClick={e=>{e.stopPropagation();patch(b.id,'published',!b.published)}}>{b.published?<Eye size={15}/>:<EyeOff size={15}/>}</button><button className="danger" title="Delete" onClick={e=>{e.stopPropagation();remove(b.id)}}><Trash2 size={15}/></button></div></div><div className="canvas-mini"><strong>{b.title||b.type}</strong><span>{b.text||'Click to edit this component'}</span>{b.mediaUrl&&<small className="media-path">Media: {b.mediaUrl}</small>}</div></div>)}{!blocks.length&&<div className="empty-state canvas-empty"><MonitorPlay/><h3>Start building this page</h3><p>Choose a component or use a template.</p><button className="btn btn-primary" onClick={()=>add('text')}><Plus/>Add Section</button></div>}</div>
   <div className={`builder-preview panel ${device}`}><div className="panel-title"><div><div className="section-kicker">LIVE PREVIEW</div><h3>{page[0].toUpperCase()+page.slice(1)} · {device}</h3></div><Eye/></div><div className="device-frame"><div className="preview-placeholder"><Sparkles/><b>Live website preview</b><span>Published blocks will render here using the same website data.</span></div></div></div>
   </section>
   <aside className="inspector panel"><div className="panel-title"><div><div className="section-kicker">PROPERTIES</div><h3>{selectedBlock?'Edit component':'Select a component'}</h3></div><Settings/></div>{selectedBlock?<><label>Title<input value={selectedBlock.title} onChange={e=>patch(selectedBlock.id,'title',e.target.value)}/></label><label>Text / content<textarea rows={6} value={selectedBlock.text} onChange={e=>patch(selectedBlock.id,'text',e.target.value)}/></label>{['image','video','media','gallery','founder'].includes(selectedBlock.type)&&<><label>Selected media<input value={selectedBlock.mediaUrl} readOnly placeholder="Choose media below"/><button className="btn btn-outline" type="button" onClick={()=>setMediaPicker(true)}><FileImage/>Choose from Media Library</button></label></>}<label>Caption<input value={selectedBlock.caption} onChange={e=>patch(selectedBlock.id,'caption',e.target.value)}/></label><div className="form-row"><label>Layout<select value={selectedBlock.layout||'wide'} onChange={e=>patch(selectedBlock.id,'layout',e.target.value)}><option value="wide">Wide</option><option value="split">Split</option><option value="card">Card</option></select></label><label>Alignment<select value={selectedBlock.align||'left'} onChange={e=>patch(selectedBlock.id,'align',e.target.value)}><option value="left">Left</option><option value="center">Center</option><option value="right">Right</option></select></label></div><label>Background<select value={selectedBlock.background||'white'} onChange={e=>patch(selectedBlock.id,'background',e.target.value)}><option value="white">White</option><option value="soft">Soft</option><option value="dark">Dark</option><option value="blue">Diamond Blue</option></select></label>{(selectedBlock.type==='stats'||selectedBlock.type==='spacer')&&<div className="form-row"><label>Value / Height<input value={selectedBlock.statValue||''} onChange={e=>patch(selectedBlock.id,'statValue',e.target.value)}/></label><label>Label<input value={selectedBlock.statLabel||''} onChange={e=>patch(selectedBlock.id,'statLabel',e.target.value)}/></label></div>}{selectedBlock.type==='cta'&&<div className="form-row"><label>Button text<input value={selectedBlock.buttonText||''} onChange={e=>patch(selectedBlock.id,'buttonText',e.target.value)}/></label><label>Button link<input value={selectedBlock.buttonUrl||''} onChange={e=>patch(selectedBlock.id,'buttonUrl',e.target.value)}/></label></div>}<label>Section name<input value={selectedBlock.section} onChange={e=>patch(selectedBlock.id,'section',e.target.value)}/></label><label className="checkline"><input type="checkbox" checked={selectedBlock.published} onChange={e=>patch(selectedBlock.id,'published',e.target.checked)}/> Visible when published</label><div className="inspector-actions"><button className="btn btn-outline" onClick={()=>duplicate(selectedBlock.id)}><Copy/>Duplicate</button><button className="btn btn-primary" onClick={()=>save(true)} disabled={busy}><Zap/>Publish Page</button></div></>:<div className="inspector-empty"><Settings/><p>Select a canvas section to edit its content, layout, media and visibility.</p></div>}</aside>
  </div>
  {mediaPicker&&<div className="modal-backdrop"><div className="modal media-picker-modal"><div className="modal-head"><div><div className="section-kicker">MEDIA LIBRARY</div><h2>Choose media</h2></div><button className="icon-btn" onClick={()=>setMediaPicker(false)}><X/></button></div><div className="modal-body"><input className="media-search" value={mediaFilter} onChange={e=>setMediaFilter(e.target.value)} placeholder="Search uploaded media…"/><div className="picker-grid">{filteredMedia.length?filteredMedia.map(m=><button key={m.id} className="picker-card" onClick={()=>chooseMedia(m)}>{m.mediaType==='image'?<img src={`${API}${m.url}`} alt={m.altText}/>:<div className="picker-video"><Video/></div>}<span><b>{m.name}</b><small>{m.page} → {m.section}</small></span></button>):<div className="empty-blog"><FileImage/><h3>No matching media</h3><p>Upload the file in Media Library first.</p></div>}</div></div></div></div>}
  {history.length>0&&<div className="revision-strip panel"><div><History/><div><b>Revision history</b><small>Restore an earlier saved version of the website.</small></div></div><select defaultValue="" onChange={e=>{if(e.target.value)restore(e.target.value)}}><option value="">Choose a revision…</option>{history.slice(0,12).map((r:any)=><option key={r.id} value={r.id}>{r.label||'Revision'} · {new Date(r.created_at||r.createdAt).toLocaleString()}</option>)}</select></div>}
  {showPreview&&<div className="modal-backdrop"><div className="modal full-preview-modal"><div className="modal-head"><div><div className="section-kicker">FULL PREVIEW</div><h2>{page[0].toUpperCase()+page.slice(1)} page</h2></div><button className="icon-btn" onClick={()=>setShowPreview(false)}><X/></button></div><div className="full-preview-body"><div className="preview-browser"><div className="preview-browser-bar"><span></span><span></span><span></span><small>Diamond Tech · {page}</small></div><div className="preview-browser-content"><PageBlocks page={page} blocks={blocks} data={data} preview/></div></div></div></div></div>}
 </div>
}

function WebsiteControl({data,setData}:{data:Site;setData:React.Dispatch<React.SetStateAction<Site>>}){const up=(k:keyof Site,v:any)=>setData(d=>({...d,[k]:v}));return <div className="editor-grid"><div className="panel"><h2>Homepage</h2><label>Eyebrow<input value={data.heroEyebrow} onChange={e=>up('heroEyebrow',e.target.value)}/></label><label>Hero heading<textarea value={data.heroTitle} onChange={e=>up('heroTitle',e.target.value)}/></label><label>Hero text<textarea value={data.heroText} onChange={e=>up('heroText',e.target.value)}/></label><label>Hero image URL<input value={data.heroImage} onChange={e=>up('heroImage',e.target.value)}/></label><h2>About</h2><label>Heading<textarea value={data.aboutTitle} onChange={e=>up('aboutTitle',e.target.value)}/></label><label>Text<textarea value={data.aboutText} onChange={e=>up('aboutText',e.target.value)}/></label></div><div className="panel"><h2>Page Headers</h2>{(['about','services','portfolio','solutions','process','blog','contact'] as const).map(k=><div key={k} className="page-editor"><b>{k[0].toUpperCase()+k.slice(1)}</b><label>Title<input value={(data.pages as any)[k+'Title']} onChange={e=>setData(d=>({...d,pages:{...d.pages,[k+'Title']:e.target.value}}))}/></label><label>Text<textarea value={(data.pages as any)[k+'Text']} onChange={e=>setData(d=>({...d,pages:{...d.pages,[k+'Text']:e.target.value}}))}/></label></div>)}<h2>Call to Action</h2><label>Title<input value={data.ctaTitle} onChange={e=>up('ctaTitle',e.target.value)}/></label><label>Text<textarea value={data.ctaText} onChange={e=>up('ctaText',e.target.value)}/></label><div className="notice-box"><Globe2/><div><b>Page control</b><p>Use Media Library to place images or videos on Home, About, Services, Portfolio, Solutions, Process, Blog and Contact sections.</p></div></div></div></div>}
function FounderControl({data,setData}:{data:Site;setData:React.Dispatch<React.SetStateAction<Site>>}){return <div className="editor-grid"><div className="panel founder-edit-preview"><img src={data.founderImage} alt="Founder"/><label>Founder image URL<input value={data.founderImage} onChange={e=>setData(d=>({...d,founderImage:e.target.value}))}/></label></div><div className="panel"><h2>Prince Diamond</h2><label>Name<input value={data.founderName} onChange={e=>setData(d=>({...d,founderName:e.target.value}))}/></label><label>Title<input value={data.founderTitle} onChange={e=>setData(d=>({...d,founderTitle:e.target.value}))}/></label><label>Biography<textarea rows={8} value={data.founderBio} onChange={e=>setData(d=>({...d,founderBio:e.target.value}))}/></label><label>Founder message<textarea rows={5} value={data.founderMessage} onChange={e=>setData(d=>({...d,founderMessage:e.target.value}))}/></label></div></div>}
function ServicesCMS({token,data,setData,search,setSearch,setToast}:{token:string;data:Site;setData:React.Dispatch<React.SetStateAction<Site>>;search:string;setSearch:(s:string)=>void;setToast:(s:string)=>void}){const[editing,setEditing]=useState<Service|null>(null);const save=async(s:Service)=>{const body={title:s.title,description:s.description,icon:s.icon,imageUrl:s.imageUrl||s.image,published:s.published,features:s.features,sortOrder:s.sort_order||0};const method=s.id.length>20?'PUT':'POST';const url=s.id.length>20?`${API}/api/admin/services/${s.id}`:`${API}/api/admin/services`;const r=await fetch(url,{method,headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify(body)});if(!r.ok){setToast('Could not save service');return}const x=await r.json();setData(d=>({...d,services:d.services.some(a=>a.id===s.id)?d.services.map(a=>a.id===s.id?x:a):[...d.services,x]}));setEditing(null);setToast('Service saved')};const del=async(id:string)=>{if(id.length>20){await fetch(`${API}/api/admin/services/${id}`,{method:'DELETE',headers:{Authorization:`Bearer ${token}`}})}setData(d=>({...d,services:d.services.filter(x=>x.id!==id)}));setToast('Service removed')};const add=()=>setEditing({id:`new-${Date.now()}`,title:'New Service',description:'',icon:'code',imageUrl:'/media/diamond-tech-logo.png',features:['Feature one'],published:false});return <div className="panel"><div className="panel-title"><div><h2>All Services</h2><p>Edit title, description, icon, image, features and publishing.</p></div><button className="btn btn-primary" onClick={add}><Plus/>Add Service</button></div><div className="cms-search"><Search/><input placeholder="Search services…" value={search} onChange={e=>setSearch(e.target.value)}/></div>{data.services.filter(s=>s.title.toLowerCase().includes(search.toLowerCase())).map(s=><div className="cms-row" key={s.id}><img src={s.imageUrl||s.image||'/media/diamond-tech-logo.png'} alt=""/><div className="grow"><b>{s.title}</b><p>{s.description}</p></div><span className={s.published?'pill live':'pill'}>{s.published?'Published':'Draft'}</span><button className="icon-btn" onClick={()=>setEditing({...s})}><Settings/></button><button className="icon-btn danger" onClick={()=>del(s.id)}><Trash2/></button></div>)}{editing&&<ServiceModal item={editing} setItem={setEditing} save={save} close={()=>setEditing(null)}/>}</div>}
function ServiceModal({item,setItem,save,close}:{item:Service;setItem:(x:Service)=>void;save:(x:Service)=>void;close:()=>void}){return <Modal title={item.title} close={close}><label>Title<input value={item.title} onChange={e=>setItem({...item,title:e.target.value})}/></label><label>Description<textarea value={item.description} onChange={e=>setItem({...item,description:e.target.value})}/></label><div className="form-row"><label>Icon<select value={item.icon} onChange={e=>setItem({...item,icon:e.target.value})}>{Object.keys(icons).map(x=><option key={x}>{x}</option>)}</select></label><label>Image URL<input value={item.imageUrl||''} onChange={e=>setItem({...item,imageUrl:e.target.value})}/></label></div><label>Features (one per line)<textarea value={item.features.join('\n')} onChange={e=>setItem({...item,features:e.target.value.split('\n').filter(Boolean)})}/></label><label className="checkline"><input type="checkbox" checked={item.published} onChange={e=>setItem({...item,published:e.target.checked})}/> Publish this service</label><button className="btn btn-primary" onClick={()=>save(item)}><Save/>Save Service</button></Modal>}
function ProjectsCMS({token,search,setSearch,setToast}:{token:string;search:string;setSearch:(s:string)=>void;setToast:(s:string)=>void}){const[items,setItems]=useState<Project[]>([]);const[editing,setEditing]=useState<Project|null>(null);const load=async()=>{const r=await fetch(`${API}/api/admin/projects`,{headers:{Authorization:`Bearer ${token}`}});if(r.ok)setItems(await r.json())};useEffect(()=>{load()},[]);const save=async(p:Project)=>{const isNew=p.id.startsWith('new-');const r=await fetch(`${API}/api/admin/projects${isNew?'':`/${p.id}`}`,{method:isNew?'POST':'PUT',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({title:p.title,category:p.category,description:p.description,content:p.content,coverUrl:p.cover_url||null,gallery:p.gallery,technologies:p.technologies,published:p.published,sortOrder:p.sort_order||0})});if(r.ok){const x=await r.json();setItems(a=>isNew?[...a,x]:a.map(z=>z.id===p.id?x:z));setEditing(null);setToast('Project saved')}};const del=async(id:string)=>{await fetch(`${API}/api/admin/projects/${id}`,{method:'DELETE',headers:{Authorization:`Bearer ${token}`}});setItems(a=>a.filter(x=>x.id!==id));setToast('Project deleted')};return <div className="panel"><div className="panel-title"><div><h2>Portfolio Projects</h2><p>Case studies, galleries, technologies, cover images and publishing.</p></div><button className="btn btn-primary" onClick={()=>setEditing({id:`new-${Date.now()}`,title:'New Project',category:'Software',description:'',content:'',gallery:[],technologies:[],published:false})}><Plus/>Add Project</button></div><div className="cms-search"><Search/><input placeholder="Search projects…" value={search} onChange={e=>setSearch(e.target.value)}/></div>{items.filter(x=>x.title.toLowerCase().includes(search.toLowerCase())).map(p=><div className="cms-row" key={p.id}><div className="grow"><b>{p.title}</b><p>{p.category} · {p.description}</p></div><span className={p.published?'pill live':'pill'}>{p.published?'Published':'Draft'}</span><button className="icon-btn" onClick={()=>setEditing({...p})}><Settings/></button><button className="icon-btn danger" onClick={()=>del(p.id)}><Trash2/></button></div>)}{editing&&<Modal title={editing.title} close={()=>setEditing(null)}><label>Project title<input value={editing.title} onChange={e=>setEditing({...editing,title:e.target.value})}/></label><div className="form-row"><label>Category<input value={editing.category} onChange={e=>setEditing({...editing,category:e.target.value})}/></label><label>Cover image URL<input value={editing.cover_url||''} onChange={e=>setEditing({...editing,cover_url:e.target.value})}/></label></div><label>Short description<textarea value={editing.description} onChange={e=>setEditing({...editing,description:e.target.value})}/></label><label>Case study content<textarea rows={8} value={editing.content} onChange={e=>setEditing({...editing,content:e.target.value})}/></label><label>Technologies (comma separated)<input value={editing.technologies.join(', ')} onChange={e=>setEditing({...editing,technologies:e.target.value.split(',').map(x=>x.trim()).filter(Boolean)})}/></label><label>Gallery URLs (one per line)<textarea value={editing.gallery.join('\n')} onChange={e=>setEditing({...editing,gallery:e.target.value.split('\n').filter(Boolean)})}/></label><label className="checkline"><input type="checkbox" checked={editing.published} onChange={e=>setEditing({...editing,published:e.target.checked})}/> Publish project</label><button className="btn btn-primary" onClick={()=>save(editing)}><Save/>Save Project</button></Modal>}</div>}
function TestimonialsCMS({token,setToast}:{token:string;setToast:(s:string)=>void}){const[items,setItems]=useState<Testimonial[]>([]);const[editing,setEditing]=useState<Testimonial|null>(null);const load=async()=>{const r=await fetch(`${API}/api/admin/testimonials`,{headers:{Authorization:`Bearer ${token}`}});if(r.ok)setItems(await r.json())};useEffect(()=>{load()},[]);const save=async(t:Testimonial)=>{const isNew=t.id.startsWith('new-');const r=await fetch(`${API}/api/admin/testimonials${isNew?'':`/${t.id}`}`,{method:isNew?'POST':'PUT',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({name:t.name,company:t.company,text:t.text,imageUrl:t.image_url||null,published:t.published})});if(r.ok){const x=await r.json();setItems(a=>isNew?[...a,x]:a.map(z=>z.id===t.id?x:z));setEditing(null);setToast('Testimonial saved')}};const del=async(id:string)=>{await fetch(`${API}/api/admin/testimonials/${id}`,{method:'DELETE',headers:{Authorization:`Bearer ${token}`}});setItems(a=>a.filter(x=>x.id!==id));setToast('Testimonial deleted')};return <div className="panel"><div className="panel-title"><div><h2>Testimonials</h2><p>Manage client stories, photos and publication state.</p></div><button className="btn btn-primary" onClick={()=>setEditing({id:`new-${Date.now()}`,name:'New Client',company:'Company',text:'Write the client testimonial here.',published:false})}><Plus/>Add Testimonial</button></div>{items.map(t=><div className="cms-row" key={t.id}><div className="avatar"><Users/></div><div className="grow"><b>{t.name}</b><p>{t.company} · {t.text}</p></div><span className={t.published?'pill live':'pill'}>{t.published?'Published':'Draft'}</span><button className="icon-btn" onClick={()=>setEditing({...t})}><Settings/></button><button className="icon-btn danger" onClick={()=>del(t.id)}><Trash2/></button></div>)}{editing&&<Modal title="Testimonial" close={()=>setEditing(null)}><label>Client name<input value={editing.name} onChange={e=>setEditing({...editing,name:e.target.value})}/></label><label>Company<input value={editing.company} onChange={e=>setEditing({...editing,company:e.target.value})}/></label><label>Review<textarea rows={6} value={editing.text} onChange={e=>setEditing({...editing,text:e.target.value})}/></label><label>Photo URL<input value={editing.image_url||''} onChange={e=>setEditing({...editing,image_url:e.target.value})}/></label><label className="checkline"><input type="checkbox" checked={editing.published} onChange={e=>setEditing({...editing,published:e.target.checked})}/> Publish</label><button className="btn btn-primary" onClick={()=>save(editing)}><Save/>Save Testimonial</button></Modal>}</div>}
function MediaCMS({token,setToast}:{token:string;setToast:(s:string)=>void}){const[items,setItems]=useState<Media[]>([]);const[type,setType]=useState<'image'|'video'>('image');const[page,setPage]=useState('Home');const[section,setSection]=useState('Hero');const[caption,setCaption]=useState('');const[alt,setAlt]=useState('');const[busy,setBusy]=useState(false);const load=async()=>{const r=await fetch(`${API}/api/admin/media`,{headers:{Authorization:`Bearer ${token}`}});if(r.ok)setItems(await r.json())};useEffect(()=>{load()},[]);const upload=async(e:any)=>{const f=e.target.files?.[0];if(!f)return;setBusy(true);const fd=new FormData();fd.append('file',f);fd.append('page',page);fd.append('section',section);fd.append('caption',caption);fd.append('altText',alt);try{const r=await fetch(`${API}/api/admin/media`,{method:'POST',headers:{Authorization:`Bearer ${token}`},body:fd});const x=await r.json();if(!r.ok)throw new Error(x.error);setItems(a=>[x,...a]);setToast('Media uploaded');setCaption('');setAlt('')}catch(e:any){setToast(e.message||'Upload failed')}finally{setBusy(false);e.currentTarget.value=''}};const remove=async(id:string)=>{await fetch(`${API}/api/admin/media/${id}`,{method:'DELETE',headers:{Authorization:`Bearer ${token}`}});setItems(a=>a.filter(x=>x.id!==id));setToast('Media deleted')};return <div className="media-manager"><div className="panel upload-panel"><div className="panel-title"><div><h2>Media Library</h2><p>Upload a photo or video, choose the page and exact section where it belongs.</p></div><Upload/></div><div className="media-types"><button className={type==='image'?'chosen':''} onClick={()=>setType('image')}><FileImage/>Photo</button><button className={type==='video'?'chosen':''} onClick={()=>setType('video')}><Video/>Video</button></div><div className="form-row"><label>Page<select value={page} onChange={e=>setPage(e.target.value)}>{['Home','About','Services','Portfolio','Solutions','Process','Blog','Contact'].map(x=><option key={x}>{x}</option>)}</select></label><label>Section<select value={section} onChange={e=>setSection(e.target.value)}>{['Hero','About','Services','Portfolio','Founder','Testimonials','CTA','Gallery','Page Header'].map(x=><option key={x}>{x}</option>)}</select></label></div><div className="form-row"><label>Caption<input value={caption} onChange={e=>setCaption(e.target.value)} placeholder="Optional caption"/></label><label>Alt text<input value={alt} onChange={e=>setAlt(e.target.value)} placeholder="Describe the image"/></label></div><label className="dropzone"><Upload/><b>{busy?'Uploading…':`Choose ${type}`}</b><span>Server-side media storage with type and size validation.</span><input type="file" accept={type==='image'?'image/*':'video/*'} onChange={upload}/></label></div><div className="media-grid">{items.map(m=><div className="media-card" key={m.id}>{m.mediaType==='image'?<img src={`${API}${m.url}`} alt={m.altText}/>:m.mediaType==='video'?<div className="video-thumb"><Video/></div>:<div className="video-thumb"><FileImage/></div>}<div><b>{m.name}</b><small>{m.page} → {m.section}</small><p>{m.caption||'No caption'}</p></div><button className="icon-btn danger" onClick={()=>remove(m.id)}><Trash2/></button></div>)}</div></div>}
function BlogCMS({token,setToast}:{token:string;setToast:(s:string)=>void}){const[items,setItems]=useState<Blog[]>([]);const[editing,setEditing]=useState<Blog|null>(null);const load=async()=>{const r=await fetch(`${API}/api/admin/blog`,{headers:{Authorization:`Bearer ${token}`}});if(r.ok)setItems(await r.json())};useEffect(()=>{load()},[]);const save=async(b:Blog)=>{const isNew=b.id.startsWith('new-');const r=await fetch(`${API}/api/admin/blog${isNew?'':`/${b.id}`}`,{method:isNew?'POST':'PUT',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({title:b.title,excerpt:b.excerpt,content:b.content,coverUrl:b.cover_url||null,author:b.author,tags:b.tags,seo:b.seo,published:b.published})});if(r.ok){const x=await r.json();setItems(a=>isNew?[x,...a]:a.map(z=>z.id===b.id?x:z));setEditing(null);setToast('Article saved')}};const del=async(id:string)=>{await fetch(`${API}/api/admin/blog/${id}`,{method:'DELETE',headers:{Authorization:`Bearer ${token}`}});setItems(a=>a.filter(x=>x.id!==id));setToast('Article deleted')};return <div className="panel"><div className="panel-title"><div><h2>Blog / News</h2><p>Publish articles, announcements, cover images, tags and SEO metadata.</p></div><button className="btn btn-primary" onClick={()=>setEditing({id:`new-${Date.now()}`,title:'New Article',excerpt:'',content:'',author:'Diamond Tech',tags:[],seo:{},published:false})}><Plus/>Add Article</button></div>{items.map(b=><div className="cms-row" key={b.id}><div className="grow"><b>{b.title}</b><p>{b.excerpt}</p></div><span className={b.published?'pill live':'pill'}>{b.published?'Published':'Draft'}</span><button className="icon-btn" onClick={()=>setEditing({...b})}><Settings/></button><button className="icon-btn danger" onClick={()=>del(b.id)}><Trash2/></button></div>)}{editing&&<Modal title={editing.title} close={()=>setEditing(null)}><label>Title<input value={editing.title} onChange={e=>setEditing({...editing,title:e.target.value})}/></label><div className="form-row"><label>Author<input value={editing.author} onChange={e=>setEditing({...editing,author:e.target.value})}/></label><label>Cover URL<input value={editing.cover_url||''} onChange={e=>setEditing({...editing,cover_url:e.target.value})}/></label></div><label>Excerpt<textarea value={editing.excerpt} onChange={e=>setEditing({...editing,excerpt:e.target.value})}/></label><label>Article content<textarea rows={12} value={editing.content} onChange={e=>setEditing({...editing,content:e.target.value})}/></label><label>Tags<input value={editing.tags.join(', ')} onChange={e=>setEditing({...editing,tags:e.target.value.split(',').map(x=>x.trim()).filter(Boolean)})}/></label><label>SEO description<textarea value={editing.seo?.description||''} onChange={e=>setEditing({...editing,seo:{...editing.seo,description:e.target.value}})}/></label><label className="checkline"><input type="checkbox" checked={editing.published} onChange={e=>setEditing({...editing,published:e.target.checked})}/> Publish article</label><button className="btn btn-primary" onClick={()=>save(editing)}><Save/>Save Article</button></Modal>}</div>}
function MessagesCMS({token}:{token:string}){const[items,setItems]=useState<any[]>([]);const load=async()=>{const r=await fetch(`${API}/api/admin/messages`,{headers:{Authorization:`Bearer ${token}`}});if(r.ok)setItems(await r.json())};useEffect(()=>{load()},[]);const status=async(id:string,s:string)=>{await fetch(`${API}/api/admin/messages/${id}`,{method:'PATCH',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({status:s})});setItems(a=>a.map(x=>x.id===id?{...x,status:s}:x))};return <div className="panel"><div className="panel-title"><div><h2>Project Messages</h2><p>Every website enquiry is stored here for follow-up.</p></div><Mail/></div>{items.length?items.map(m=><div className="message-card" key={m.id}><div><b>{m.full_name}</b><span>{m.email} · {m.phone}</span><small>{new Date(m.created_at).toLocaleString()}</small></div><span className="pill">{m.status}</span><p><b>{m.service}</b> · {m.budget}<br/>{m.message}</p><div className="message-actions"><button className="btn btn-outline" onClick={()=>status(m.id,'read')}>Mark Read</button><a className="btn btn-outline" href={`mailto:${m.email}`}>Reply</a><button className="btn btn-primary" onClick={()=>status(m.id,'replied')}>Replied</button></div></div>):<div className="empty-blog"><Mail/><h3>No project messages yet.</h3><p>New contact form submissions will appear here.</p></div>}</div>}
function AuditCMS({token}:{token:string}){const[items,setItems]=useState<any[]>([]);useEffect(()=>{fetch(`${API}/api/admin/audit`,{headers:{Authorization:`Bearer ${token}`}}).then(r=>r.ok?r.json():[]).then(setItems)},[]);return <div className="panel"><div className="panel-title"><div><h2>Audit Trail</h2><p>Security and content-change history for administrator actions.</p></div><History/></div>{items.map(x=><div className="audit-row" key={x.id}><span>{new Date(x.created_at).toLocaleString()}</span><b>{x.action}</b><span>{x.entity}</span><small>{x.username||'system'}</small></div>)}</div>}
function SEOControl({data,setData}:{data:Site;setData:React.Dispatch<React.SetStateAction<Site>>}){return <div className="panel"><h2>SEO Control Center</h2><p>Global search and sharing metadata for the website.</p><label>Site title<input value={data.siteTitle} onChange={e=>setData(d=>({...d,siteTitle:e.target.value}))}/></label><label>Meta description<textarea value={data.metaDescription} onChange={e=>setData(d=>({...d,metaDescription:e.target.value}))}/></label><div className="notice-box"><Search/><div><b>SEO architecture</b><p>Per-page metadata, Open Graph images, sitemap.xml, robots.txt and canonical URLs are ready to be extended.</p></div></div></div>}
function SettingsControl({data,setData}:{data:Site;setData:React.Dispatch<React.SetStateAction<Site>>}){return <div className="panel"><h2>Global Settings</h2><label>Phone / WhatsApp<input value={data.phone} onChange={e=>setData(d=>({...d,phone:e.target.value}))}/></label><label>Email<input value={data.email} onChange={e=>setData(d=>({...d,email:e.target.value}))}/></label><div className="notice-box"><ShieldCheck/><div><b>Production security</b><p>Authentication uses expiring JWT sessions, password hashing, protected admin routes, rate-limited login, validation, Helmet security headers and an audit trail.</p></div></div></div>}
function Modal({title,close,children}:{title:string;close:()=>void;children:any}){return <div className="modal-backdrop"><div className="modal"><div className="modal-head"><div><div className="section-kicker">EDIT CONTENT</div><h2>{title}</h2></div><button className="icon-btn" onClick={close}><X/></button></div><div className="modal-body">{children}</div></div></div>}

export default App;
